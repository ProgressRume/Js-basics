1.  What runs JavaScript outside the browser?
Node, a C++ program that includes googles v8 Javascript engine runs JavaScript outside the browser

2. Name 5 things Javascript can do.

Javascript can help you build full blown Web
It can help build Mobile applications
It can also help create Real-time Networking Applications like Chats, Video streaming services and command line tools
Javascript can also be used to create games
It can also help display popup windows and dialog boxes

3. When was ECMAScript first released?

The first version of ECMAScript was released in 1997

4. How do you log to the console?

Open Chrome
Right Click on any empty area, go to inspect and select console tab.


5.  List the tech fields that use JavaScript, for example FrontEnd.
website administration, web application development, frontend.

6. Differentiate between Statically typed and Dynamically typed programming languages and give examples of languages that fall under each category
A statically-typed language is one where variable types are known at compile time. 
In statistically-typed language, types must be expressly indicated by the programmer. Examples include, Java, C, or C++
Dynamically-typed languages on the other hand, are those where the interpreter assigns variables a type at runtime based on the variable's value at the time. Examples include, Javascript

7. Differentiate between the JavaScript data types ‘null’ and ‘undefined'
In JavaScript, undefined means a variable has been declared, but no value has been assigned a value.
Null on the other hand, an assignment value i.e you can assign it to a variable.

